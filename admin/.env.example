# SQLite (dev) : fichier créé dans prisma/dev.db. À l'exécution, chemin résolu en absolu (voir src/lib/prisma.ts).
DATABASE_URL="file:./dev.db"
# Puis : npm run db:push && npm run db:seed (depuis racine : npm run db:push && npm run db:seed)
NEXTAUTH_SECRET="change-me-in-production"
NEXTAUTH_URL="http://localhost:3001"
