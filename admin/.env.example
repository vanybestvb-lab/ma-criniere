# PostgreSQL (obligatoire : Neon, Supabase ou local). Voir admin/SETUP-DATABASE.md.
# Exemple Neon : postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
# Optionnel
NEXTAUTH_SECRET="change-me-in-production"
NEXTAUTH_URL="http://localhost:3001"
# Mode démo (sans BDD) : connexion admin@ma-criniere.com / admin123, données mock
# NEXT_PUBLIC_DEMO_MODE=true
# Origine autorisée pour les appels API depuis la boutique (CORS)
CORS_ORIGIN="http://localhost:3000"
# URL du backend NestJS (optionnel : si défini, produits/stock/livraisons viennent de l'API)
BACKEND_URL="http://localhost:3002"
